# Мои скрипты по кибербезопасности (My Cybersecurity Scripts)

Этот репозиторий — моя личная коллекция Python-скриптов, написанных в процессе обучения пентестингу и сетевой безопасности. Каждый инструмент создан для понимания фундаментальных концепций атак и защиты.

---

## 📜 Содержание

* [Инструменты для атаки](#-инструменты-для-атаки)
    * [ARP Spoofer (`arpSpoof.py`)](#arp-spoofer-arpspoofpy)
    * [MAC Flooder (`mac_flooder.py`)](#mac-flooder-mac_flooderpy)
* [Инструменты для разведки](#-инструменты-для-разведки)
    * [TCP SYN Scanner (`syn_scanner.py`)](#tcp-syn-scanner-syn_scannerpy)
* [Инструменты для защиты / Обнаружения](#-инструменты-для-защиты--обнаружения)
    * [ARP Spoofing Detector (`arpDetector.py`)](#arp-spoofing-detector-arpdetectorpy)
    * [XMas Scan Detector (`xmas_detector.py`)](#xmas-scan-detector-xmas_detectorpy)

---

### 🛡️ Дисклеймер

Все скрипты в этом репозитории предназначены исключительно **для образовательных целей** и использования в легальных лабораторных средах (например, TryHackMe, Metasploitable) или в сетях, на тестирование которых у вас есть явное разрешение.

---

## ⚔️ Инструменты для атаки

### ARP Spoofer (`arpSpoof.py`)

Интерактивный ARP-спуфер для проведения атак "Человек посередине" (MITM).

**Предназначение:** Скрипт перенаправляет трафик между устройством-жертвой и маршрутизатором через машину атакующего, отправляя ложные ARP-ответы обоим. Автоматически включает и отключает IP-форвардинг в системе.

**Использование:**

`sudo python3 arpSpoof.py <IP-адрес_жертвы> <IP-адрес_маршрутизатора>`

**Пример:**

`sudo python3 arpSpoof.py 192.168.1.100 192.168.1.1`

Скрипт может работать в двух режимах:
1.  **Автоматический (ответ 'n'):** Сам определяет MAC-адреса целей.
2.  **Ручной (ответ 'y'):** Позволяет ввести MAC-адреса вручную для ускорения запуска.

---

### MAC Flooder (`mac_flooder.py`)

Скрипт на Python/Scapy для проведения атаки МАС-флудинга (переполнения CAM-таблицы коммутатора).

**Предназначение:** Отправляет непрерывный поток пакетов со случайными MAC-адресами источников. Это заставляет коммутатор войти в "аварийный режим" (fail-open) и вести себя как хаб, рассылая весь трафик на все порты.

**Использование:**

`sudo python3 mac_flooder.py`

**🛡️ Методы защиты:** На управляемых коммутаторах эта атака предотвращается функцией **Port Security**, которая ограничивает количество MAC-адресов на порт и блокирует его при нарушении.

---

## 🗺️ Инструменты для разведки

### TCP SYN Scanner (`syn_scanner.py`)

Легковесный сканер портов, использующий "полуоткрытую" технику SYN-сканирования (эквивалент `nmap -sS`).

**Предназначение:** Быстро определяет состояние портов (Open, Closed, Filtered) на целевом хосте, не завершая полное TCP-рукопожатие. Это позволяет найти потенциальные точки входа (веб-серверы, SSH, FTP и т.д.) на начальном этапе разведки.

**Использование:**

`sudo python3 syn_scanner.py <IP-адрес_цели>`

**Пример:**

`sudo python3 syn_scanner.py 192.168.1.100`

---

## 🛡️ Инструменты для защиты / Обнаружения

### ARP Spoofing Detector (`arpDetector.py`)

Эвристический детектор ARP-спуфинг атак.

**Предназначение:** Скрипт отслеживает ARP-трафик и строит карту `IP:MAC`. Если он замечает, что MAC-адрес, который уже есть в базе, вдруг объявляет себя с новым IP-адересом, он выводит предупреждение о возможной MITM-атаке.

**Использование:**

`sudo python3 arpDetector.py`

---

### XMas Scan Detector (`xmas_detector.py`)

Простая система обнаружения вторжений (IDS), настроенная на поиск "Рождественского" сканирования.

**Предназначение:** "Рождественское" сканирование (`nmap -sX`) — это стелс-атака, использующая пакеты с флагами `FIN`, `PSH` и `URG`. Этот скрипт ищет в трафике пакеты с этой "сигнатурой" (комбинацией флагов `0x29`) и бьет тревогу при их обнаружении.

**Использование:**

`sudo python3 xmas_detector.py`

**Для тестирования (на другой машине):**

`sudo nmap -sX <IP-адрес_детектора>`
